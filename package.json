{
  "scripts": {
    "todo:install": "cd ToDo-List && npm run install",
    "todo:build": "cd ToDo-List && npm run build",
    "todo:start": "cd ToDo-List && npm run start",
    "predeploy": "cd ToDo-List && npm run build",
    "deploy": " cd ToDo-List && gh-pages -d build"
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}
